language: ruby
rvm:
  - 2.5
script: bundle exec jekyll build
cache: bundler

branches:
  only:
      - master
deploy:
  provider: script
  skip_cleanup: true
  script: gem install aliyun-sdk && ruby scripts/deploy.rb
  keep_history: true
  on:
    branch: master 

notifications:
  email: false
